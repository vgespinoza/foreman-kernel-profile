---
# vars file for cca-so-config-kernel-parameters

sendPortal: true
sendSgl: false

kernel_values:
  - {param: "vm.swappiness", value: 20, enforce: true}
  - {param: "vm.dirty_ratio", value: 30, enforce: true}
